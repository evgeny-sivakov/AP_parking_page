import { Playfair_Display, Lato } from 'next/font/google';

export const playfair_display = Playfair_Display({ subsets: ['cyrillic'] });
export const lato = Lato({ subsets: ['latin'], weight: ['300'] });
